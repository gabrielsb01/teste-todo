import { ListContent } from './ListContent'
import { ListHeader } from './ListHeader'
import { ListRoot } from './ListRoot'

export const List = {
  Root: ListRoot,
  Header: ListHeader,
  Content: ListContent,
}
